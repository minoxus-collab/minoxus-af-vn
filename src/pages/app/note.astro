---
// src/pages/app/note.astro
export const prerender = false;

import AppLayout from '../../layout/AppLayout.astro';
import AppContainer from '../../components/app/AppContainer';
---

<AppLayout title="Minox Note | Quản lý dự án">
  <!-- Gọi component React và bắt buộc dùng client:load để kích hoạt JS trên trình duyệt -->
  <AppContainer client:load />
</AppLayout>